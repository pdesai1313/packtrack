# Copy this to .env.local and fill in the values

# Leave empty for local dev (Vite proxy handles it)
# Set to your Render backend URL for production, e.g:
# VITE_API_BASE_URL=https://pack-lottery-api.onrender.com
VITE_API_BASE_URL=
