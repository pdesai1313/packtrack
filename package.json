{
  "name": "pack-lottery",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
    "seed": "cd backend && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
